//Question # 11
//Think of your favorite mode of transportation
var transporation = ["car", "bike", "cycle", "bus", "auto", "greeline bus", "suzuki"];
for (var i = 0; i < transporation.length; i++) {
    console.log('i would like to own a ' + transporation[i]);
}
