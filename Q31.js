//QUESTION # 31
var user = ['sana', 'muntaha', 'javeria', 'fatima', 'seerat',];
if (user.length === 0) {
    console.log('we would to this find some users! ');
}
else {
    user = [];
    console.log('all users have been removed' + user.length);
}
